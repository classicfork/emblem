export default function ManageMemorialPortalRoute() {
    // const data = useLoaderData<typeof loader>();
    // console.log(data);

    return (
      <div>
        <p>Manage Memorial!</p>
      </div>
    );
  }